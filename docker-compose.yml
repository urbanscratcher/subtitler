services:
  subtitler:
    build: .
    ports:
      - "3011:5175"
    environment:
      HOST: 0.0.0.0
      PORT: 5175
    networks:
      - default
      - proxy

networks:
  proxy:
    external: true
